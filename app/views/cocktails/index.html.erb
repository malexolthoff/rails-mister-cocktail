<% @cocktails.each do |cocktail| %>
  <h2> <%= cocktail.name %></h2>
  <h4><%= link_to "Get the deets", cocktail_path(cocktail) %></h4>
<% end %>

<%= link_to "Add more cocktails", new_cocktail_path %>
